    <p>
    <div class="review">
      <div class="poster">
        <% p review %>
        Posted by <%= review.user.email %> on <% review.created_at %>
      </div>
      <p class="comment">
        <%= review.comment %>
      </p>
    </div>
    <%= link_to "Delete Review", product_review_path(review.product, review), method: :delete, data: {confirm: "Are you sure you want to delete this review?"}  %>
