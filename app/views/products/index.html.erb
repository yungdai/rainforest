<!-- this is the main index.html page for the index method inside the pictures_controller.rb file -->
<h1>Product Directory</h1>
<!-- this is a link to the /products/new.html.erb file -->

<%= link_to "New Product", "/products/new" %>


<!-- Notice we are using a helper, link_to, to make a link. The first parameter is the text and the second parameter
is the URL clicking the text takes you to. In this case clicking on "Back to Pictures" takes you to the path
pictures_url, which is /pictures. -->
<% @products.each do |product| %>
    <p><%= product.name %>, <%= product.description %></p>
    <%= product.price_in_cents %>
<% end %>