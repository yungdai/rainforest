<h1>Products</h1>
<%= link_to 'Back', products_path %>
<br/>
<p><%= @product.name %></p>
<p><%= @product.description %></p>
<p>$<%= @product.formatted_price %></p>



<% if current_user %>
    <%= link_to "Edit", "/products/#{@product.id}/edit"%> |
    <%= link_to "Delete", @product, method: :delete, data: {confirm: "Are you sure you want to delete this product?"}  %>
    <h3>Reviews</h3>

    <p>Reviews for <%= @product.name %></p>

    <% @product.reviews.each do |review| %>
        <p>
          <%= review.comment %>
          <br/>
          <% if review.user != nil %>
              <!--This was crated from the products/show.html.erb -->
            <em> by <%= review.user.email %></em>
            Added on: <%= review.created_at %>
           <!--  link_to "Delete", @review.destroy, method: :delete, data: {confirm: "Are you sure you want to delete this review?"} -->
          <% end %>
        </p>
    <% end %>

<!-- this is where you create a new review -->
    <h4>New Review</h4>

    <%= render 'review_form' %>

<% else %>
    <p>Please <%= link_to "log in", new_session_path %> to add a review.</p>
<% end %>
